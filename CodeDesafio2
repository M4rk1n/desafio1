km = int(input("Digite a quantidade de km's percorrido pelo carro: "))

dias = int(input("Digite a quantidade de dias de aluguel do carro: "))



print("valor total a pagar: ", km * 0.15 + dias * 60)
